[package]
name = "proc-macro-missing-constructor"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
publish = false

[workspace]
members = []

[dependencies]
sim = { version = "0.1.0", path = "../../..", default-features = false, features = ["core", "shape", "proc-macros"] }
