Materialize bounded evidence first, bind exact checker, binding, subject, and
scope values, then invoke the corresponding public pack function. A declared
future scope returns `UnimplementedPack`; malformed identity strings, wrong
scopes, and missing evidence return a named refusal.
