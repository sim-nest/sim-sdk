# Evaluate and browse TypeScript notation

Evaluate compiler-independent notation through the unchanged JavaScript policy, browse a faithful annotation as Shape metadata, and confirm that checker-dependent annotation syntax remains an explicit unsupported feature rather than a runtime check.
