# JVM lambda interop through the SDK

Use only the `sim` facade to inspect the admitted compiler protocol, create the
loader-local generated-class space, reach the Java-to-SIM Callable projection,
and name the capability required by the SIM-to-Java functional adapter.
