# Adapt A Reference Device At Two Rates

This recipe runs the modeled reference device without hardware. It encodes one
content-rate Scene, adapts it through the rich profile and the compact glance
profile, and reports the coalesced device-rate samples and stale-frame marker.
