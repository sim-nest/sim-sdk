# Swap A Modeled Device Route

This recipe runs a route change for a modeled device edge session. It reports
that the session id, ledger reference, consent binding, event history, and
surface peer registration survive when the route changes.
