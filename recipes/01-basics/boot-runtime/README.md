# Boot The Facade Runtime

This recipe records the smallest public-facade boot path: create a runtime from
the umbrella `sim` crate, install the core runtime, and evaluate a simple form
through the Lisp codec and default number domain.
