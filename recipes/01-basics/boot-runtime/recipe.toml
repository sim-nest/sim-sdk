id = "boot-runtime"
title = "Boot the facade runtime"
codec = "lisp"
setup = "input.lisp"
purpose = "README.md"
expected = "expected.txt"
order = 10
tags = ["sdk", "facade", "boot", "offline", "deterministic"]
requires = ["core", "shape", "codec/lisp", "numbers/f64"]
network = false
