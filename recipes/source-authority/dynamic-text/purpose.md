# Admit dynamic text with shared source authority

Construct the trusted policy, caller requirements, and diminished allowed
powers once in `sim::source_authority::SourceAuthority`. The guest-facing
dynamic policy receives that indivisible value; it cannot recover omitted
powers or create a second admission model.
