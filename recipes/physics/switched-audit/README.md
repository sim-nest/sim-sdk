# Audit a switched boundary

Load the layered physics runtime, declare one switch event and a signed
electrical boundary, solve through an injected method, and retain independent
work, storage, and uncertainty lanes. Certification is emitted only when the
enclosure proves the threshold; otherwise the verdict remains unresolved. The
result becomes an immutable finding and is browsed through its projection.
