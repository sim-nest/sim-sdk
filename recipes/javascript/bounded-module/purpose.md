# Evaluate a bounded JavaScript module

Parse and directly evaluate module source, settle a promise only through an explicit bounded microtask checkpoint, and inspect the deliberate Node and compiler gaps.
