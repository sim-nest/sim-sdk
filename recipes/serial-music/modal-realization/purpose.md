Freeze one row statement as an immutable serial plan, then realize it through
the registry-backed modal surface exported by `sim::serial_music::adaptation`.
The source plan stays unchanged while the sounding result can vary by scale.
