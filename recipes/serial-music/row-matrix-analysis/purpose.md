Build one strict twelve-tone row, enumerate its family, and render its matrix
through `sim::serial_music::theory` only. This keeps downstream code on the
stable SDK surface while the owner crates keep the implementation details.
