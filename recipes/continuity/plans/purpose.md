# Compose continuity plans as data

Expose the canonical continuity owner through the SDK and keep carrier-only,
walk, desk, optional-Halo, rejected watch-only, and fictional future endpoint
variants as versioned data. Removing every optional role leaves the complete
Android-rooted minimum unchanged.
