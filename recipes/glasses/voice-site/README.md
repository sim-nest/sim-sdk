# Recognize Voice Through A Modeled Site

This recipe passes a canned microphone chunk reference through a modeled ASR
site export. The call requires kernel `glasses/mic` authority and a visible
receipt bound to the active glasses session, then returns a standard Intent.
