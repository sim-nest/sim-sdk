# First light

This checked SDK specimen composes the canonical expedition book, capability-pack
closure, continuity journal, and injected directory. It completes and exports a
synthetic expedition, deletes every derived or optional artifact, then reconstructs
the result offline from durable bytes. Unsupported routing and a refused effect stay
visible as conditions in the replayed journal; neither a model endpoint nor a device
is required.
